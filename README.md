# 실적분석 (중국법인 재무실적 종합 분석)

회계관리·예산관리·자금집행 세 모듈의 데이터를 종합해서 법인별 재무실적과 리스크를 보여주는
대시보드입니다. 관리부 ERP 통합시스템의 마지막(네 번째) 데이터 모듈로, 회계관리 → 예산관리 →
자금집행 → **실적분석** 파이프라인의 종착점입니다.

**이 모듈은 별도의 데이터 입력이 없습니다.** 세 모듈이 이미 저장한 데이터를 조인해서 보여주는
순수 조회/분석 대시보드이며, 새 테이블을 만들지 않습니다.

회계관리, 예산관리, 자금집행, 접대비와 동일하게 바닐라 HTML/JS/CSS + Supabase + GitHub Pages
구조입니다. Supabase 프로젝트는 회계관리/예산관리/자금집행과 **같은 관리부 ERP 전용 프로젝트를
공유**합니다 (접대비는 별도 프로젝트). 차트는 Chart.js를 vendor로 내려받아 CDN 없이
사용합니다.

## v2 업데이트 (지점 드릴다운 + 목표 기준 예산섹션)
- **지점 드릴다운**: index.html/view.html에 지점 선택이 추가되어, 법인 전체 또는 특정 지점의
  수익성 추이/재무안정성을 볼 수 있습니다 (`get_profitability_series`/`get_stability_series`에
  `p_office` 파라미터 추가). 지점 선택 시 자금위험요약 섹션은 숨겨집니다(자금집행은 법인 단위만
  지원).
- **예산 대비 실적 → 목표달성현황으로 교체**: 기존 PL 계정별 예산 비교 대신, 목표영업이익
  달성률과 일반관리비 예산 진행현황을 보여줍니다 (`get_budget_variance_summary` 재설계).
- **목표실적 관리표 누계화**: 선택한 월까지 1월부터 누계로 집계합니다 (인원수는 평균).
- **관리자 화면에 목표실적 관리표 추가**: 연도/법인/지점을 선택해 당해 누계·전년 동기·전년동기대비
  증감까지 스크린샷 제공 리포트와 동일한 구성으로 조회/다운로드할 수 있습니다. 전년 데이터가 아직
  없으면 해당 구간은 비어 있습니다 (구조만 준비, 데이터는 추후 자동으로 채워짐).

## 4가지 분석 지표 (월간/분기/연간 토글 지원)
1. **수익성 추이**: 매출/영업이익/순이익 및 영업이익률/순이익률 (회계관리 PL 데이터 기반)
2. **재무안정성**: 부채비율/유동비율 (회계관리 B/S 데이터 기반)
3. **예산 대비 실적**: 달성률 + 예산 초과 계정 목록 (예산관리 데이터 재사용)
4. **자금 위험 요약**: 시재 증감률/총 차입금잔액/배당가능금액 (자금집행 데이터 재사용)

월간 데이터를 저장하고, 화면에서 분기/연간으로 자동 집계합니다. **흐름지표(매출/이익)는 기간 내
합산, 저량지표(비율/잔액)는 기간 말월 값**을 사용합니다 (재무 원칙상 합산하면 안 되는 지표를
실수로 합산하지 않도록 프론트 로직에서 구분).

## 5번째 지표: 목표실적 관리표 (v2 추가, 월간 기준 - 분기/연간 토글 미지원)
법인 산하 지점별로 목표영업이익 대비 실적을 종합하는 표입니다. `get_target_performance_report`가
아래 데이터를 지점별로 조인해서 반환합니다 (신규 테이블 없이 세 모듈 데이터를 그대로 재사용):
- 목표영업이익: 예산관리 `bgt_target_profit`
- 매출/매출원가/매출이익/일반관리비/영업이익/당기순이익: 회계관리 PL(한국) (`statement_type='PL_KR'`)
- 인원수/접대비: 회계관리 `acct_pl_kr_extra` (접대비는 접대비 앱이 별도 Supabase 프로젝트라
  자동 연동이 안 되어 지점이 회계관리 쪽에 수기로 입력한 값)
- 출장비: 예산관리 `bgt_ga_lines` (category='travel', kind='actual')

⚠ 이 표는 월간 스냅샷만 지원합니다. 인원수 같은 저량지표를 분기/연간으로 어떻게 집계할지(기간말
값 vs 평균) 등 추가 설계가 필요해 분기/연간 토글은 이번 범위에서 제외했습니다 (향후 보완 가능).

## 구성
- `docs/` — GitHub Pages로 배포되는 프론트엔드
  - `index.html` — 법인/적용년도월/접근키 입력
  - `view.html` — 읽기 전용: 4개 섹션 + 월간/분기/연간 토글 + 수익성 추이 차트
  - `admin.html` — [본사용] 전체 법인 비교 테이블 + 막대차트 + 엑셀 다운로드
- `supabase/` — 함수 정의만 (`schema.sql`, 신규 테이블 없음). **회계관리·예산관리·자금집행의
  schema.sql이 모두 먼저 적용되어 있어야** 정상 동작합니다.

## 배포 방법
`supabase/README-deploy.md`를 따라 Supabase에 함수를 적용한 뒤,
`docs/js/config.js`의 `SUPABASE_URL`/`SUPABASE_ANON_KEY`를 채우고 GitHub Pages로 배포하세요.

## 로컬 실행
```
python -m http.server 8093 --directory docs
```

## 주의: 계정코드/법인 목록 하드코딩
이 모듈의 RPC는 회계관리 실제 계정코드(PL_KR 포함)와 6개 법인 목록을 하드코딩으로 참조합니다.
COA 교체 또는 법인 변경 시 `supabase/README-deploy.md`를 참고해 `schema.sql`을 다시 적용하세요.

## 커스터마이징
- 법인 목록: `docs/js/config.js`
- 다국어 문구: `docs/js/i18n.js`
- Chart.js 버전: `docs/vendor/chart.umd.js` (jsdelivr에서 재다운로드)
